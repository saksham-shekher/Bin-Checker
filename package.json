{
  "name": "bin-checker",
  "version": "1.0.0",
  "description": "Node.js BIN Checker API – identify card brand, lengths, and validation metadata from a BIN/IIN prefix",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "validate": "node scripts/validate.js",
    "create-card": "node scripts/create-card.js",
    "api:start": "node api/server.js",
    "api:test": "node --test api/*.test.js"
  },
  "keywords": [
    "bin",
    "bin-checker",
    "creditcard",
    "card-validation",
    "api"
  ],
  "license": "MIT",
  "devDependencies": {
    "js-yaml": "^4.1.1"
  }
}
